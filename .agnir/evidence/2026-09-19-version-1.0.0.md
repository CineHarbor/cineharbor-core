# Core 1.0.0 version alignment — 2026-09-19

Predecessor source `e2bb2c6cab5cf620ab4eef34e05b254b26dc3139` passed the full CI matrix twice in runs `35420232092` and `35420280323`.

Release alignment changes:
- `[workspace.package].version`: `0.1.0` → `1.0.0`.
- `Cargo.lock` updates exactly the eight owned workspace packages to `1.0.0`: cineharbor-addon-host, cineharbor-core-web, cineharbor-core, cineharbor-download, cineharbor-local-service, cineharbor-profile, cineharbor-storage and cineharbor-sync.
- The sibling `cineharbor-addon-protocol` and `cineharbor-addon-sdk` lock entries remain at their pinned sibling revision/version until that repository performs its own version alignment.

The change creates a new source revision; the predecessor passes are historical input only. Required acceptance is complete PR CI followed by two successful main runs on the merged SHA.
