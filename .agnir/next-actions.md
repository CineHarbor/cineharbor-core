# cineharbor-core Next Actions

0. **提交并推送本次 Agnir 初始化**（`AGNIR.yaml` / `AGENTS.md` / `.agnir/` / README 段），当前均为未提交改动。

1. P1→P3 长成 Stremio-core 等价物。
2. P3 起 `cineharbor-local-service` 依赖 `cineharbor-addon-sdk`（git/crates.io 引入），见门面仓 `docs/adr/0003`。
3. `cargo check --workspace` / `cargo test --workspace`。
