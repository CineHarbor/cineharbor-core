[package]
name = "cineharbor-addon-host"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# 跨仓依赖：未发布 crates.io 前用相对 path（ADR-0003）。CI 需同时 checkout 兄弟仓。
cineharbor-addon-sdk = { path = "../../../cineharbor-addon-sdk/crates/cineharbor-addon-sdk" }
axum.workspace = true
serde.workspace = true

[dev-dependencies]
async-trait = "0.1.89"
reqwest.workspace = true
serde_json.workspace = true
tokio.workspace = true