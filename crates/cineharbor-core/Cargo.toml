[package]
name = "cineharbor-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# 跨仓依赖：crates.io 未发布前用相对 path（ADR-0003）。CI 需同时 checkout 兄弟仓。
cineharbor-addon-protocol = { path = "../../../cineharbor-addon-sdk/crates/cineharbor-addon-protocol" }
async-trait.workspace = true
http = "1"
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
url.workspace = true
reqwest = { workspace = true, optional = true }
rusqlite = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }

[features]
native-http = ["dep:reqwest"]
native-storage = ["dep:rusqlite", "dep:tokio"]

[dev-dependencies]
axum.workspace = true
tokio.workspace = true